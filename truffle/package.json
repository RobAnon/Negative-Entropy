{
  "name": "truffle",
  "version": "1.0.0",
  "description": "We are using Truffle here, if you don't have in installed, `npm install -g truffle` You also might want to read [truffle's guide](https://www.trufflesuite.com/docs/truffle/overview) first.",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "@openzeppelin/contracts": "^3.4.1",
    "@openzeppelin/test-environment": "^0.1.9",
    "@openzeppelin/test-helpers": "^0.5.10",
    "@truffle/hdwallet-provider": "^1.1.1",
    "chai": "^4.3.4",
    "dotenv": "^8.2.0",
    "mocha": "^8.3.2",
    "truffle-plugin-verify": "^0.5.7",
    "web3": "^1.3.4"
  },
  "scripts": {
    "test": "mocha --exit --recursive --timeout 5000"
  },
  "author": "",
  "license": "ISC"
}
